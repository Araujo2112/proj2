package com.example.projetov2.repository;

import com.example.projetov2.model.TipoRelatorio;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TipoRelatorioRepository extends JpaRepository<TipoRelatorio, Integer> {
}